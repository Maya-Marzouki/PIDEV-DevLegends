{% extends 'base.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}
    <h1>Répondez au quiz</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}
