{% extends 'baseAdmin.html.twig' %}

{% block body %}
<div class="row d-flex justify-content-center">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-block text-center">
                <h4 class="card-title">QR Code des Consultations</h4>
                <h6 class="card-subtitle">Contient toutes les consultations</h6>

                {# Afficher le QR code #}
                <div class="mt-4">
                    <img src="{{ qrCodeUrl }}" alt="QR Code des Consultations" class="img-fluid">
                </div>
        {# Bouton "Ajouter une Réponse" en bas à droite #}
        <style>
            .btn-custom-green {
                background-color: #28a745 !important; /* Vert Bootstrap */
                color: white !important; /* Texte blanc */
                font-weight: bold;
            }
        </style>
                {# Boutons en bas à droite #}
                <div class="d-flex justify-content-end mt-3">
                    <a href="{{ path('consultation_index') }}" class="btn btn-custom-green mr-2">Retour à la liste</a>
                    <a href="{{ qrCodeUrl }}" download="consultations_qr_code.png" class="btn btn-custom-green mr-2"> Télécharger le QR Code
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}